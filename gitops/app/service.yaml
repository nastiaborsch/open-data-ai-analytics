apiVersion: v1
kind: Service
metadata:
  name: open-data-web-service
  namespace: open-data-app
spec:
  type: NodePort
  selector:
    app: open-data-web
  ports:
    - protocol: TCP
      port: 5678
      targetPort: 5678
      nodePort: 30080