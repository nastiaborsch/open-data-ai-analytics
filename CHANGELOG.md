# CHANGELOG

## v0.1.0 - Initial project version

### Added
- Initial project structure (`README.md`, `.gitignore`, `data/README.md`, `notebooks/`, `src/`, `reports/figures/`)
- `data_load` module (`src/data_load.py`) for loading open data from CSV/ZIP
- `data_quality_analysis` module (`src/data_quality_analysis.py`) for basic data quality checks
- `data_research` module (`src/data_research.py`) for exploratory analysis and aggregations
- `visualization` module (`src/visualization.py`) for generating charts and saving figures

### Git workflow
- Feature branches created and merged via Pull Requests
- Merge conflict in `README.md` was created intentionally and resolved correctly